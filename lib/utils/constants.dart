import 'package:flutter/material.dart';

class AppConstants {
  static const Color primaryColor = Color(0xFF1ce783);
  static const Color secondaryColor = Color(0xFF111111);
  static const double smallScreenWidth = 600.0;
  static const double mediumScreenWidth = 1000.0;
}
